#include<iostream>
using namespace std;

int main()
{
    double a, b, c;
    cin >> a >> b >> c;
    if(a > 0 && b > 0 && c > 0)
        cout << "Ca ba so deu duong";
    else    cout << "It nhat mot so am";
    return 0;
}
